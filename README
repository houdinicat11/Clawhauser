README

// todo: everything

packages:
	
	npm init -y
	npm install discord.js
	npm install nodemon --save-dev
	npm install dotenv
	npm install discord.js ffmpeg-binaries opusscript ytdl-core --save
	node index.js
	npm install node-opus
	ffmpeg-20190604-d81913e-win64-static
	npm install discord.js-musicbot-addon
	// npm install discord.js ffmpeg-binaries opusscript ytdl-core --save - not needed
	// npm install dotenv - not needed
	npm install ytdl-core
	npm install ffmpeg-binaries
	npm install ffmpeg-static
	npm install ffmpeg
	npm i ffmpeg-binaries
	npm install discord.js opusscript
	npm install discord.js node-opus
	npm install Node.js
	npm install node.js
	choco install ffmpeg
	cmd/powershell
	cmd/powershell choco install ffmpeg
	// choco install ffmpeg - not needed
	choco upgrade ffmpeg
	